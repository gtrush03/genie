#!/usr/bin/env node
// Vercel Deployer
// Takes a directory → runs vercel deploy --yes --prod → returns live URL
// Usage: node deploy-vercel.mjs --dir /tmp/genie/xxx --name project-name

// TODO: Phase 2 implementation
